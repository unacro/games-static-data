{
	"name": "games-static-data-script",
	"version": "1.1.0",
	"workspaces": ["packages/*"],
	"scripts": {
		"dev": "bun --filter '*' dev",
		"test": "bun --filter 'utils' dev",
		"app": "bun --filter 'database' app"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
